# ── Aliases: homebrew  (sysup — the full update pipeline — is autoloaded from functions/)
alias brewclean='brew cleanup && brew autoremove'
alias brewlist='brew list --formula'
alias brewdeps='brew deps --tree --installed'
