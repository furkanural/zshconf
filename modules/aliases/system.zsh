# ── Aliases: system  (OS-coupled ones — ip/localip/free/f — live in modules/os/)
alias ports='lsof -PiTCP -sTCP:LISTEN'
alias sizeof='du -sh'
alias grep='grep --color=auto'
alias df='df -h'
alias du='du -h'
