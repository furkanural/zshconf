---
name: Bug report
about: Something broke or misbehaves
---

**What happened / what did you expect?**

**Environment**
- OS + version (macOS 15 / Ubuntu 24.04 / WSL ...):
- Terminal (Ghostty / iTerm2 / GNOME Terminal ...):
- `zsh --version`:
- Optional tools installed (output of `command -v eza fzf bat fd rg zoxide starship vivid mise`):

**Startup output**
Anything printed on a fresh `exec zsh` (especially stderr):

**Overlay involved?**
Does it still happen with `~/.config/zsh/` temporarily moved aside?
