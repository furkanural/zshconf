**What & why**

**Checklist**
- [ ] `just test` passes (and `just test-bare` if I touched degradation guards)
- [ ] Fits the scope policy in CONTRIBUTING.md (opinionated config, not a framework — no new knobs)
- [ ] Load-order comments in `init.zsh` still true if I touched the manifest
